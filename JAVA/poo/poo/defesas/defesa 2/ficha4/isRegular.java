

public boolean isRegular()
{
	
}

//http://www.allegro.cc/forums/thread/591460
public Angle(Point o, Point a, Point a)
{
	
acos( ((ax * bx) + (ay * by)) / (sqrt(ax*ax + ay*ay) * sqrt(bx*bx + by*by))
}


// cos ang = v(u)*v(v)/|v(u)|*|v(v)|
// ang = arc cos(cos ang)